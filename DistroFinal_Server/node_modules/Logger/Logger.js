var fs = require('fs');


class Logger{
    constructor(path){
        this.path = path;
        //this.file = fs.appendFile(path);
    }
    addLog(mensagem) {
        // usar
         fs.writeFileSync(this.path,mensagem,'utf8');
    }
    lerLog(){
        // retorna os dados do logger
    }


}

module.exports = Logger;